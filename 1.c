#include<stdio.h>
int main()
{
   const int N = 55;
   char str[10][55];
   int len[10];
   scanf("%s",str[0]);
   len[0] = strlen(str[0]);
      for(int i=1;i<4;i++)
      {
      scanf("%s",str[i]);
      len[i] = strlen(str[i]);
      }
   }

